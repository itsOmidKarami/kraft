---
work_item_ids: [6a9a79c948b548bdb72f6f2c41a4a745]
node_id: human_review
hook_point: on.human_review.requested
kind: review_briefs
title: CLI and UI polish — 13 small-hole fixes across CLI, API, and settings UI
---

## Status

This run is a retry of `mr_checks` — no code changes. The human's steer noted
this is the same flake three times in a row on this branch, matching a pattern
from an earlier branch that took ~7 retries. All 13 beads' work was already
committed in prior runs (`962b859`, `bb7211c`); this session only re-verified
the tree is clean and re-ran the targeted test suites.

- Backend targeted tests (doctor, repos, watching, fixtures, settings_api,
  store, api): 279 passed.
- Frontend unit tests (`npx vitest run`): 284 passed, 31 files.
- `git status`: clean, nothing to commit.

## What changed

Two commits carry the work:

- `962b859` — the 13-bead batch itself:
  - `Kraft-tmsv`: chain template textarea height fix (`frontend/src/styles.css`).
  - `Kraft-9r4q`: dev vite proxy now forwards `/steering`, `/intake`, `/notify`
    (`frontend/vite.config.ts`, `frontend/src/vite.proxy.test.ts`).
  - `Kraft-8cfh`: removed the redundant `/settings` redirect `useEffect`
    (`frontend/src/views/Settings.tsx`).
  - `Kraft-hbsv`: work item title is now editable; PATCH accepts title
    (`src/kraft/api.py`, `frontend/src/views/WorkItemDetail.tsx`, `api.ts`).
  - `Kraft-cy30`: long titles no longer 502 on item create
    (`src/kraft/api.py`, `tests/test_api.py`).
  - `Kraft-8okl`: `_cmd_watch` calls the correct client method, not `_get`
    (`src/kraft/cli.py`, `src/kraft/client.py`, `tests/test_cli_watching.py`).
  - `Kraft-7qgb`: `repo disconnect` can remove a repo registered under a
    worktree path (`src/kraft/store.py`, `tests/test_cli_repos.py`).
  - `Kraft-e9a`/`Kraft-17p`/`Kraft-oah` (the steering trio, done together as
    instructed): async steering save no longer does blocking directory I/O
    (`src/kraft/api.py`), the steering editor gained diff/revert
    (`frontend/src/components/DraftDiff.tsx`, `frontend/src/diff.ts`,
    `Settings.tsx`), and the DELETE removed-state reload path now has a test
    (`frontend/src/views/Settings.test.tsx`, `tests/test_settings_api.py`).
  - `Kraft-lqzx`: `doctor` now checks the kraft MCP server is registered
    (`src/kraft/doctor.py`, `tests/test_cli_doctor.py`).
  - `Kraft-map`: `fake-claude.sh` only writes its artifact when it actually
    reports success (`fixtures/fake-claude.sh`, `tests/test_fixtures.py`).
  - `Kraft-3e6e`: added a visual (rendered, not raw-JSON) validator for a
    chain template, built last as planned so it could validate the spliced
    chain from Kraft-bpkj.
- `bb7211c` — a small follow-up fix keeping the work item title's `<h2>` text
  pure, moving Edit to a sibling element (touches
  `frontend/src/views/WorkItemDetail.tsx` only).

Full diff: 32 files, +3733/-87, including a test per bead in
`tests/` and `frontend/src/*.test.tsx`.

## What CI said

Not observed directly this session — this run only retried the `mr_checks`
node per the human's steer. Backend and frontend test suites re-run locally
here both pass in full (279 + 284 tests). No lint run this session.

## What was deliberately left out

Nothing new scoped out this run; scope was fixed in the spec/plan commits
(`e2cc520`, `9e49ebf`) from earlier in the chain. `Kraft-bwp9` (the umbrella
epic) is intentionally not closed by this branch — it tracks the batch, not
a deliverable itself.

## What I'm unsure about

- I did not re-run `just lint` this session (only targeted pytest + vitest),
  since no files changed and lint was presumably green in the run that
  produced `962b859`/`bb7211c`.
- The recurring `mr_checks` flake itself is outside this session's visibility
  — nothing in the diff looks related to CI infra, so it's likely an
  environment/runner issue rather than something introduced by this branch.
