---
work_item_ids: [64a546d5f6ea4d32a4e8601859990b00]
node_id: verify
hook_point: on.implementation.start
worker_session_id: cc8462a51e3c40c5bd223d2b774be5d2
---

# Verify fix: chain_definition leaked from client.get_work_item

`just test` failed on `on.test.run`: the previous attempt's commit
(`Attachments reachable from CLI/MCP...`) added `"chain_definition"` to the
`keep` tuple in `client.get_work_item`, reintroducing a field the client is
supposed to trim away (`kraft show --json` deliberately strips the chain,
per `Kraft-9rs` — see `tests/test_client_read.py`). That broke
`test_get_work_item_names_the_next_node`.

Fix:
- `src/kraft/client.py`: dropped `"chain_definition"` back out of `keep`.
  `_next_node_id` already computed its answer from the untrimmed `item`
  before the dict comprehension runs, so nothing else needed to change.
- `tests/test_cli_verbs.py::test_create_attaches_a_spec_from_the_flag`:
  this new test (added in the same prior commit) asserted the trimmed gate
  by reading `chain_definition` off `view show --json`'s output — which no
  longer carries it. Switched that one assertion to fetch the untrimmed
  item straight from the API (same pattern `test_get_work_item_names_the_next_node`
  uses), so it still proves the attached spec trimmed `spec_approval` off
  the chain.

Verified: `just lint` clean, `just test` green (1399 passed, 4 skipped) on
two full runs.
