---
work_item_ids: [b65465d9a714499abb64562c620680e3]
node_id: plan
hook_point: on.plan.requested
worker_session_id: a27bdbe93691449897bf5d2ccf8f0da9
---

# Plan session: refresh worktree base on resume

Read the approved spec (`.engineering/specs/b65465d9a714499abb64562c620680e3.md`)
and wrote `.engineering/plans/b65465d9a714499abb64562c620680e3.md`, a 3-task
plan implementing it:

1. New `refresh_worktree_base` builtin in `src/kraft/builtins.py` (beside
   `ensure_worktree`), with 5 unit tests in `tests/test_builtins.py` covering
   no-worktree, already-up-to-date, already-pushed, a real rebase, and a
   conflict-with-abort.
2. Wire it into `resume_work_item` (`src/kraft/api.py`), plus a one-line
   docstring fix on `store.set_base_ref`, plus 4 integration tests in
   `tests/test_pause_resume.py` (rebase-onto-moved-HEAD, dirty-tree skip,
   conflict → `needs_human`, already-pushed skip).
3. Same wiring into `retry_work_item`, plus 1 integration test in
   `tests/test_api_retry_open_log.py`.

Verified every code reference against the actual source (line numbers,
function signatures, existing test harness helpers `_git`/`make_repo`/
`_client`/`_running_agent`, the `quick-task` chain template, and
`fake-claude.sh`'s behavior) rather than trusting the spec's line numbers
blind — they all still matched. Each task's plan section names its exact
failing test code and exact implementation code, in full. No open questions;
the spec was implementation-ready and this plan just breaks it into ordered,
independently-testable tasks and pins the parts it left as prose (test
bodies, exact insertion points, import ordering) into runnable code.
