---
work_item_ids: [b65465d9a714499abb64562c620680e3]
node_id: chain_review
hook_point: on.chain.review_ready
kind: chain_reviews
title: Default chain fits; tail emitted unchanged
---
{
  "status": "ready_for_approval",
  "revised_chain_nodes": [
    { "id": "env_setup", "tasks": ["on.env.prepare"], "gate_after": null, "fix_loop": null },
    { "id": "implementation", "tasks": ["on.implementation.start"], "gate_after": null, "fix_loop": null },
    { "id": "verify", "tasks": ["on.test.run", "on.review.local.run"], "gate_after": null, "fix_loop": "verify_fix_loop" },
    { "id": "open_mr", "tasks": ["on.mr.open"], "gate_after": null, "fix_loop": null },
    { "id": "mr_checks", "tasks": ["on.ci.poll", "on.review.mr.run"], "gate_after": null, "fix_loop": null, "on_failure": ["on.mr_checks.repair"] },
    { "id": "human_review", "tasks": ["on.mr.sync", "on.human_review.requested"], "gate_after": "human_review_approval", "fix_loop": null },
    { "id": "merge", "tasks": ["on.merge"], "gate_after": null, "fix_loop": null }
  ],
  "rationale": "No change: the default chain's tail is emitted exactly as loaded, including mr_checks' on_failure repair hook. The plan is three tasks inside one repo -- a new refresh_worktree_base builtin in src/kraft/builtins.py plus call sites in resume_work_item and retry_work_item in src/kraft/api.py -- with its own pytest coverage named in the spec's Verification section (tests/test_builtins.py, tests/test_pause_resume.py, tests/test_api_retry_open_log.py), so verify's on.test.run has real work and nothing is redundant to drop. Nothing in spec or plan touches authentication, sessions, tokens, secrets or permission checks; there is no schema or migration; the item is single-repo, so no cross-repo ordering concern. The plan has no risk section naming a failure mode the chain misses. One thing worth the approver's eye rather than a chain edit: the change makes POST /resume and POST /retry able to flip an item to needs_human on a rebase conflict, a behaviour change on two existing HTTP endpoints, and the allowed hook set (registry.yaml) registers no contract/compat or security-review hook -- the closest available check is on.review.mr.run, which is a builtin noop here, so a contract task cannot be added without approximating it with the wrong hook. The spec argues that status change is the intended outcome and the endpoints' shapes are unchanged, so leaving the chain alone is right; flagging it so a human can decide otherwise."
}
