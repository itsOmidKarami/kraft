---
work_item_ids: [d824aa0661d54ee88e4c4c4d2f6cfdb1]
node_id: chain_review
hook_point: on.chain.review_ready
kind: chain_reviews
title: Default chain fits the forge.py fix; tail emitted unchanged
---
{
  "status": "ready_for_approval",
  "revised_chain_nodes": [
    { "id": "env_setup", "tasks": ["on.env.prepare"], "gate_after": null, "fix_loop": null },
    { "id": "implementation", "tasks": ["on.implementation.start"], "gate_after": null, "fix_loop": null },
    { "id": "verify", "tasks": ["on.test.run", "on.review.local.run"], "gate_after": null, "fix_loop": "verify_fix_loop" },
    { "id": "open_mr", "tasks": ["on.mr.open"], "gate_after": null, "fix_loop": null },
    { "id": "mr_checks", "tasks": ["on.ci.poll", "on.review.mr.run"], "gate_after": null, "fix_loop": null, "on_failure": ["on.mr_checks.repair"] },
    { "id": "human_review", "tasks": ["on.mr.sync", "on.human_review.requested"], "gate_after": "human_review_approval", "fix_loop": null },
    { "id": "merge", "tasks": ["on.merge"], "gate_after": null, "fix_loop": null }
  ],
  "rationale": "No change: the default chain's tail is emitted exactly as loaded, including mr_checks' on_failure repair hook. The plan is two independent tasks in a single repo, both confined to src/kraft/adapters/forge.py and tests/test_forge.py (plan: 'no other file changes, no template/registry changes'), each with its own failing-tests-first section and a named command, so verify's on.test.run has real work and no tier is redundant enough to drop. Nothing touches authentication, sessions, tokens, secrets or permission checks; there is no schema or migration; single repo, so no cross-repo ordering concern. _merge_state's new `branch` parameter is private to GlabCli and defaulted, and the plan states ci_status is its only caller, so there is no public API or wire-format shape for a contract task to check even if one were registered. The plan's own risk material (the 'Out of scope' section: the human_review/mr_sync race, the narrow find_mr-to-merge window, MERGE_VERIFY_TIMEOUT tuning) names nothing the chain is supposed to catch. One thing for the approver's eye rather than a chain edit: this item edits the very handlers its own tail runs -- on.ci.poll at mr_checks and on.merge at merge. Those nodes execute against the installed Kraft, not this worktree, so the run is not self-hosting the new code and the tail is safe as written; but it does mean the fix's real-world behaviour (merge re-polling CI after mr_sync's push) is first exercised on the *next* work item, not this one, and only tests/test_forge.py evidences it here."
}
