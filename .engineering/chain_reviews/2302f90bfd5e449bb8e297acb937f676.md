---
work_item_ids: [2302f90bfd5e449bb8e297acb937f676]
node_id: chain_review
hook_point: on.chain.review_ready
kind: chain_reviews
title: Chain unchanged — single-repo internal fix, standard tail fits
---

{
  "status": "ready_for_approval",
  "revised_chain_nodes": [
    { "id": "env_setup",      "tasks": ["on.env.prepare"],                    "gate_after": null,                     "fix_loop": null },
    { "id": "implementation", "tasks": ["on.implementation.start"],           "gate_after": null,                     "fix_loop": null },
    { "id": "verify",         "tasks": ["on.test.run", "on.review.local.run"], "gate_after": null,                    "fix_loop": "verify_fix_loop" },
    { "id": "open_mr",        "tasks": ["on.mr.open"],                        "gate_after": null,                     "fix_loop": null },
    { "id": "mr_checks",      "tasks": ["on.ci.poll", "on.review.mr.run"],    "gate_after": null,                     "fix_loop": null, "on_failure": ["on.mr_checks.repair"] },
    { "id": "human_review",   "tasks": ["on.mr.sync", "on.human_review.requested"], "gate_after": "human_review_approval", "fix_loop": null },
    { "id": "merge",          "tasks": ["on.merge"],                          "gate_after": null,                     "fix_loop": null }
  ],
  "rationale": "The plan is a three-file change inside one repo: a new `_pin_identity` helper in `src/kraft/builtins.py` called once from `ensure_worktree`, a docstring fix in the same file, a comment fix in `src/kraft/executor.py`, and one new case in `tests/test_builtins.py`. Nothing in it meets an add trigger — no authentication, sessions, tokens or permission checks; no schema or migration; no public API or wire format (`ensure_worktree` and `_pin_identity` are internal to Kraft's own executor path); a single repo, so no cross-repo ordering. Nothing meets a lighten trigger either: the plan changes executable code with real branching (the missing-key raise), and its own Step 3 specifies a targeted pytest run, which is exactly what `verify`'s `on.test.run` executes — dropping it would remove the only check of the new failure path. The spec's Non-goals section explicitly rules out chain-level enforcement (\"No enforcement elsewhere in the chain telling agents 'never fabricate a git identity'\"), which is a direct statement that the chain is not where this work is done. `mr_checks`'s `on_failure: [on.mr_checks.repair]` is carried through verbatim rather than dropped, since it is present in the chain as loaded and omitting it would delete the repair path."
}
