---
work_item_ids: [beffc97911cf4c12ac905d30c776a403]
node_id: chain_review
hook_point: on.chain.review_ready
kind: chain_reviews
title: Chain unchanged for SPA shell auth bypass fix; no security-review hook is registered
---
{"status": "ready_for_approval",
 "revised_chain_nodes": [
   {"id": "env_setup", "tasks": ["on.env.prepare"], "gate_after": null, "fix_loop": null},
   {"id": "implementation", "tasks": ["on.implementation.start"], "gate_after": null, "fix_loop": null},
   {"id": "verify", "tasks": ["on.test.run", "on.review.local.run"], "gate_after": null, "fix_loop": "verify_fix_loop"},
   {"id": "open_mr", "tasks": ["on.mr.open"], "gate_after": null, "fix_loop": null},
   {"id": "mr_checks", "tasks": ["on.ci.poll", "on.review.mr.run"], "gate_after": null, "fix_loop": null, "on_failure": ["on.mr_checks.repair"]},
   {"id": "human_review", "tasks": ["on.mr.sync", "on.human_review.requested"], "gate_after": "human_review_approval", "fix_loop": null},
   {"id": "merge", "tasks": ["on.merge"], "gate_after": null, "fix_loop": null}
 ],
 "rationale": "Chain emitted unchanged, with one thing you should weigh before approving. The plan edits the authentication middleware itself: step 3 widens `_authenticate`'s bypass condition in src/kraft/api.py to let through every GET whose path is not an API path, and step 4 deletes the Sec-Fetch-Dest / FileResponse block that stood behind it. That is squarely the 'touches authentication or permission checks' case where a security-review task belongs in `verify` — the correctness of the fix rests entirely on `_is_api_path` classifying every gated GET route as API, since anything it misclassifies becomes publicly readable. No security-review hook is registered in this repo's allowed hook set (on.env.prepare, on.spec.requested, on.plan.requested, on.implementation.start, on.test.run, on.chain.review_ready, on.review.local.run, on.mr.open, on.ci.poll, on.review.mr.run, on.mr.sync, on.human_review.requested, on.merge, on.mr_checks.repair), so I have not approximated one with a different task. Worth knowing: `verify`'s second task, on.review.local.run, is a builtin noop, so pytest plus the human_review gate are the only real checks this auth change will get. The spec's own test list (mutating verb still 401s, /api/<unknown> still 404s rather than falling through to the shell) is the thing to confirm actually landed. Nothing was added, dropped, or reordered; mr_checks keeps its existing on_failure repair task."}
