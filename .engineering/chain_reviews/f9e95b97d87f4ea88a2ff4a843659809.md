---
work_item_ids: [f9e95b97d87f4ea88a2ff4a843659809]
node_id: chain_review
hook_point: on.chain.review_ready
kind: chain_reviews
title: Default chain fits; no security-review or migration hook registered to add
---
{
  "status": "ready_for_approval",
  "revised_chain_nodes": [
    {
      "id": "env_setup",
      "tasks": [
        "on.env.prepare"
      ],
      "gate_after": null,
      "fix_loop": null,
      "on_failure": null,
      "reject_to": null
    },
    {
      "id": "implementation",
      "tasks": [
        "on.implementation.start"
      ],
      "gate_after": null,
      "fix_loop": null,
      "on_failure": null,
      "reject_to": null
    },
    {
      "id": "verify",
      "tasks": [
        "on.test.run",
        "on.review.local.run"
      ],
      "gate_after": null,
      "fix_loop": "verify_fix_loop",
      "on_failure": null,
      "reject_to": null
    },
    {
      "id": "open_mr",
      "tasks": [
        "on.mr.open"
      ],
      "gate_after": null,
      "fix_loop": null,
      "on_failure": null,
      "reject_to": null
    },
    {
      "id": "mr_checks",
      "tasks": [
        "on.ci.poll",
        "on.review.mr.run"
      ],
      "gate_after": null,
      "fix_loop": null,
      "on_failure": [
        "on.mr_checks.repair"
      ],
      "reject_to": null
    },
    {
      "id": "human_review",
      "tasks": [
        "on.mr.sync",
        "on.human_review.requested"
      ],
      "gate_after": "human_review_approval",
      "fix_loop": null,
      "on_failure": null,
      "reject_to": null
    },
    {
      "id": "merge",
      "tasks": [
        "on.merge"
      ],
      "gate_after": null,
      "fix_loop": null,
      "on_failure": null,
      "reject_to": null
    }
  ],
  "rationale": "Chain unchanged. The plan is an ordinary in-repo feature: a schema migration (18 -> 19, one nullable column), a new escalate.py dispatch, one POST endpoint, one CLI verb, one MCP tool, and pytest coverage for each -- exactly the shape the default chain was picked for, and the plan's own test section runs against `on.test.run`. Two lines would justify an addition if the hook existed. First, spec Architecture item 2: the escalation dispatch deliberately omits KRAFT_WORK_ITEM_ID so `client.resolve_context()` resolves the agent as origin=\"user\" rather than \"worker\", which is what lets it past `_forbid_self_action` and run `kraft item retry` on its own item. That is a permission-boundary change (by construction rather than by editing the guard), and a security-review task on `verify` would be the right addition -- but this repo's allowed hook set has no security-review hook registered (`on.review.local.run` is bound to the `noop` builtin), and approximating it with a different hook would be worse than saying so. Second, the migration: no migration/verification hook is registered either. Both are flagged here so the human at this gate can decide whether to hold the item or review those two points by hand at human_review; the gate at human_review is intact and is where that review lands. Tail emitted verbatim, including mr_checks' on_failure repair path, which the splice replaces wholesale and would otherwise be dropped."
}
