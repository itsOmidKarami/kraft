---
work_item_ids: [04b8b4c011fc4c4fbd602286d9f66765]
node_id: chain_review
hook_point: on.chain.review_ready
kind: chain_reviews
title: Default chain kept unchanged; migration and gate-authority checks have no registered hook
---
{
  "status": "ready_for_approval",
  "revised_chain_nodes": [
    { "id": "env_setup", "tasks": ["on.env.prepare"], "gate_after": null, "fix_loop": null },
    { "id": "implementation", "tasks": ["on.implementation.start"], "gate_after": null, "fix_loop": null },
    { "id": "verify", "tasks": ["on.test.run", "on.review.local.run"], "gate_after": null, "fix_loop": "verify_fix_loop" },
    { "id": "open_mr", "tasks": ["on.mr.open"], "gate_after": null, "fix_loop": null },
    { "id": "mr_checks", "tasks": ["on.ci.poll", "on.review.mr.run"], "gate_after": null, "fix_loop": null },
    { "id": "human_review", "tasks": ["on.mr.sync", "on.human_review.requested"], "gate_after": "human_review_approval", "fix_loop": null },
    { "id": "merge", "tasks": ["on.merge"], "gate_after": null, "fix_loop": null }
  ],
  "rationale": "Chain emitted unchanged, with two things the human should weigh. First, the plan's Task 2 adds a schema migration (`ALTER TABLE work_items ADD COLUMN auto_gate`, SCHEMA_VERSION 20 -> 21); the heuristic for a schema change is a migration-verification task before `open_mr`, and no such hook is registered in this repo's allowed hook set, so I did not approximate it with another one. The plan does pin the migration in tests (existing rows read back `0`), which is the closest the current chain gets. Second, this item changes who may clear a gate: the spec's Approach section rests its entire safety story on `client._forbid_self_action` refusing a worker's approve/reject/pause/retry on its own item, and the plan's Task 5 tests that guard directly. That is authorization-shaped work, and the heuristic would add a security-review task to `verify` -- but the allowed hook set has no security-review hook either (`on.review.local.run` and `on.review.mr.run` are both `builtin: noop` here), so `verify` currently measures this change with `uv run pytest` and nothing else. Everything else fits: the work is single-repo Python with per-task tests, `human_review` and its gate are intact ahead of `merge`, and nothing in the plan makes a tail node inapplicable. If the human wants a machine check on the migration or on the gate-authority guard beyond the plan's own tests, that is a registry change, not a chain change."
}
