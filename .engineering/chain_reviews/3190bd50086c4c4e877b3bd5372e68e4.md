---
work_item_ids: [3190bd50086c4c4e877b3bd5372e68e4]
node_id: chain_review
hook_point: on.chain.review_ready
kind: chain_reviews
title: Default chain fits submodule merge-request implementation; unchanged tail
---
{
  "status": "ready_for_approval",
  "revised_chain_nodes": [
    { "id": "env_setup", "tasks": ["on.env.prepare"], "gate_after": null, "fix_loop": null },
    { "id": "implementation", "tasks": ["on.implementation.start"], "gate_after": null, "fix_loop": null },
    { "id": "verify", "tasks": ["on.test.run", "on.review.local.run"], "gate_after": null, "fix_loop": "verify_fix_loop" },
    { "id": "open_mr", "tasks": ["on.mr.open"], "gate_after": null, "fix_loop": null },
    { "id": "mr_checks", "tasks": ["on.ci.poll", "on.review.mr.run"], "gate_after": null, "fix_loop": null },
    { "id": "human_review", "tasks": ["on.mr.sync", "on.human_review.requested"], "gate_after": "human_review_approval", "fix_loop": null },
    { "id": "merge", "tasks": ["on.merge"], "gate_after": null, "fix_loop": null }
  ],
  "rationale": "Chain emitted unchanged. The plan is a single-repo change to the Kraft repo itself (Python backend plus one frontend file), so the default tail's open_mr/mr_checks/human_review/merge sequence already fits: no cross-repo ordering to confirm, no auth/secrets/permission surface, no public wire format broken (store.repos_for changes signature but every caller is in-tree, plan Task 1). Two things a human at this gate should know, neither actionable by me because no matching hook is registered in this repo's allowed hook set. First, Task 1 adds a SQLite schema change (SCHEMA_VERSION 19, migration 18 on work_item_repos, plan lines 108-160) and there is no migration-verification hook available — the plan leans on db.py's own import-time migration-continuity assert plus tests/test_store.py instead. Second, Task 2 changes frontend/src/views/WorkItemDetail.tsx with a vitest test (npm --prefix frontend test), while the only registered test hook, on.test.run, runs `uv run pytest -q` and will never execute that test; there is no frontend-test hook to add. If either gap matters, the fix is registering a hook, not reshaping this chain."
}
