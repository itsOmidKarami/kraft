
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

.kraft-run/

# Python build artifacts
__pycache__/
*.egg-info/
dist/
build/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.uv-cache/

# macOS
.DS_Store

# Frontend (Vite SPA)
node_modules/
*.tsbuildinfo
frontend/node_modules/
frontend/dist/
frontend/playwright-report/
frontend/test-results/
frontend/e2e-shots/

# Written by Settings → Access. Everything else the Settings screens write is
# versioned on purpose; this one holds a password hash and a bind address that
# are per-machine, so it stays local.
templates/access.yaml

# Dev instance home (`just dev`) and the build outputs `just install` copies in
.dev/
src/kraft/_bundled/
