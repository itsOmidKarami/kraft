<!-- What this changes, and why -- the diff already shows what moved. -->



## Changelog

<!-- One user-facing line, e.g. "Fix: ...". Leave empty for release::none. An empty section lists the title instead. -->

## Release impact

Apply exactly one label: `release::major`, `release::minor`, `release::patch`, or `release::none`.
`release::none` is a first-class answer for docs, comments, CI config, and test-only
changes. See [CONTRIBUTING.md](CONTRIBUTING.md#pull-requests-and-release-labels).
<!-- This file becomes the PR/MR body, so the link is relative to the repo root, not to .github/. -->
