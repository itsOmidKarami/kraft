---
title: Kraft for VS Code
description: Install the Kraft extension to clear gates, review a branch's diff, and edit Kraft config files without leaving VS Code.
---

The Kraft extension shows the board in the sidebar and lets you clear what
Kraft is waiting on you for from the editor: approve or reject a gate, review a
work item's branch, and edit Kraft's YAML config files with errors flagged as
you type.

## Install

Install **Kraft** from the VS Code Marketplace or Open VSX. To install a
release by hand, download `kraft-X.Y.Z.vsix` from the
[GitHub release](https://github.com/itsOmidKarami/kraft/releases) and run:

```bash
code --install-extension kraft-X.Y.Z.vsix
```

The extension needs a running Kraft on the same machine:

```bash
kraft admin start
```

For completion and instant structure checks in config files, also install
[Red Hat YAML](https://marketplace.visualstudio.com/items?itemName=redhat.vscode-yaml).
The extension offers it once if it is missing.

## Clear a gate

When a gate opens, VS Code shows a notification with **Open**, **Approve** and
**Reject**. Open shows the gate's document in a tab; approve and reject are also
in that tab's title bar and on the item in the sidebar.

## Review a branch

Run **Review Changes** on an item to open its branch in the multi-file diff
editor. Findings from the review agents show as diagnostics on the lines they
name. Add a comment on any line, then run **Submit Review**: your comments go
to the agent as one note, as a reject at a gate or as a steer on a paused item.
Approving instead asks before it discards your comments.

## Edit config files

Files under `$KRAFT_HOME/templates` (`policy.yaml`, `harnesses.yaml`, `chains/*.yaml`
and the rest) get completion from Kraft's JSON Schemas, and the running Kraft
checks each edit and flags the line at fault. On a chain file, **Show resolved
chain** previews the chain after inheritance. On save, the extension offers to
reload Kraft, or to restart it for files Kraft reads only at startup.

## Settings

| Setting | What it does |
|---|---|
| `kraft.url` | Where Kraft listens. Empty reads `bind` and `port` from `access.yaml`, then `KRAFT_HOST` and `KRAFT_PORT`. |
| `kraft.notifications` | `all`, `gates` or `off`: which states raise a notification. |

If Kraft's version does not match the extension's, the extension turns read-only:
the board still updates and every action is disabled until you update one of them.
